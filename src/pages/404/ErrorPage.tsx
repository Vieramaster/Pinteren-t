import { ErrorImage } from "./ErrorImage";

const ErrorPage = () => {
  return (
    <main className="w-full bg-surface h-screen pt-20 relative overflow-hidden">
      <ErrorImage className="size-full"/>
    </main>
  );
};

export default ErrorPage;
